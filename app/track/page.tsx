'use client';import {useState} from 'react';export default function Track(){const [id,setId]=useState('');const [found,setFound]=useState<any>(null);return <main className="max-w-3xl mx-auto px-5 py-20"><p className="gold uppercase tracking-[.2em] text-xs">Order service</p><h1 className="serif text-6xl mt-3">Track your order</h1><p className="text-stone-600 mt-5">Enter your DONN ALLENO order number.</p><div className="flex gap-2 mt-8"><input className="input" value={id} onChange={e=>setId(e.target.value)} placeholder="e.g. DA123456"/><button className="btn btn-dark" onClick={()=>setFound(JSON.parse(localStorage.getItem('da-last-order')||'null'))}>Track</button></div>{found&&<div className="card p-7 mt-10"><div className="flex justify-between"><span>Order {found.order}</span><b>{found.status}</b></div><div className="grid grid-cols-5 gap-2 mt-10">{['Received','Confirmed','Processing','Shipped','Delivered'].map((x,i)=><div key={x} className="text-center"><div className={`w-8 h-8 rounded-full mx-auto ${i===0?'bg-black':'bg-stone-200'}`}/><small className="block mt-2">{x}</small></div>)}</div></div>}</main>}
