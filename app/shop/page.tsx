'use client';import {Suspense} from 'react';import {useSearchParams} from 'next/navigation';import ProductCard from '@/components/ProductCard';import {products} from '@/lib/products';import Link from 'next/link';
function ShopContent(){const q=useSearchParams().get('category');const list=q?products.filter(p=>p.category===q):products;return <main className="max-w-7xl mx-auto px-5 py-16"><p className="gold uppercase tracking-[.25em] text-xs font-bold">Collection</p><h1 className="serif text-6xl mt-2">{q||'Shop All'}</h1><div className="flex gap-3 mt-8 flex-wrap">{['All','Senator','Suits'].map(x=><Link key={x} className={`btn ${((x==='All'&&!q)||x===q)?'btn-dark':''}`} href={x==='All'?'/shop':`/shop?category=${x}`}>{x}</Link>)}</div><div className="grid sm:grid-cols-2 lg:grid-cols-4 gap-7 mt-12">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div></main>}

export default function Shop(){return <Suspense fallback={<main className="max-w-7xl mx-auto px-5 py-16">Loading collection...</main>}><ShopContent /></Suspense>}
